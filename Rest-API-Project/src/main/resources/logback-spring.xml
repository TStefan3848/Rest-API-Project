<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{20} - %msg%n"/>
    <property name="APP_LOG_ROOT" value="Logs"/>

    <appender name="statisticsAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${APP_LOG_ROOT}/RequestStatistics.log</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${APP_LOG_ROOT}/RequestStatistics-%i.log</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>10</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>10MB</maxFileSize>
        </triggeringPolicy>
    </appender>

    <include resource="org/springframework/boot/logging/logback/base.xml"/>
    <logger name="stefan.toth.RestAPIProject.config" level="INFO" additivity="false">
        <appender-ref ref="statisticsAppender"/>
        <appender-ref ref="CONSOLE"/>
    </logger>
</configuration>